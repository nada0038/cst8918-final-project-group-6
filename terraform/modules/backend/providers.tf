terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # NOTE: Do NOT use `use_oidc` here because you are running this locally via `az login`.
}
